# 智慧烟感管理平台 — 项目结构与功能说明

> 本文档面向团队开发成员，详细说明项目的整体结构组成、各模块职责与完整功能，便于快速理解与上手。

## 1. 项目概述

智慧烟感管理平台是一个面向社区/园区的**火灾预警与设备管理后端系统**。系统通过 MQTT 接收烟感设备的实时上报数据，基于**多参数联合判定 + 消抖**的规则引擎进行分级告警（预警/火警），并支持设备管理、心跳离线检测、低电量检测、告警升级、警情统计与 RAG 大模型问答。

### 1.1 技术栈

| 类别 | 技术 |
|------|------|
| 语言 / 框架 | Java 21、Spring Boot 3.5.x |
| ORM | MyBatis-Plus 3.5.x |
| 数据库 | PostgreSQL + pgvector 扩展（向量检索） |
| 硬件通信 | MQTT（Eclipse Paho） |
| 实时推送 | WebSocket（STOMP 协议） |
| 认证 | JWT（jjwt） |
| 工具 | Hutool、Lombok |

### 1.2 核心特性

- **分级告警**：预警（WARN）/ 火警（FIRE）/ 离线 / 故障 / 低电量 五类等级
- **多参数联合判定**：烟雾浓度 + 温度 + CO 浓度交叉验证，降低误报
- **消抖机制**：连续 N 次超阈值才触发告警，过滤瞬时抖动
- **告警升级**：火警超时未确认自动升级通知
- **事件驱动解耦**：MQTT 网关与业务逻辑通过领域事件解耦，无循环依赖
- **RBAC 权限**：居民 / 小区管理员 / 系统管理员 / 消防员 四角色 + 小区数据隔离
- **RAG 问答**：基于 pgvector 向量检索 + 大模型的消防知识问答

---

## 2. 系统架构

### 2.1 分层架构

```
┌─────────────────────────────────────────────────────┐
│  Controller 层（HTTP 接口 / RESTful）                  │
│  参数校验、权限注解、统一返回 Result                     │
├─────────────────────────────────────────────────────┤
│  Service 层（业务逻辑）                                │
│  规则引擎、告警、设备、用户、RAG、控制日志               │
├─────────────────────────────────────────────────────┤
│  Mapper 层（MyBatis-Plus 数据访问）                    │
│  CRUD、分页、pgvector 向量检索                         │
├─────────────────────────────────────────────────────┤
│  PostgreSQL（含 pgvector 扩展）                        │
└─────────────────────────────────────────────────────┘
```

### 2.2 双通道通信 + 事件驱动

系统存在三条通信链路，通过**领域事件**将它们解耦：

| 通道 | 方向 | 用途 |
|------|------|------|
| HTTP | 客户端 → 服务端 | 管理类接口（注册/登录/设备 CRUD/查询） |
| MQTT | 硬件 → 服务端 | 设备实时上报（烟雾/心跳/自检/告警） |
| WebSocket | 服务端 → 客户端 | 实时推送（烟雾数据、告警、在线状态） |

关键设计：`MqttGateway` 收到硬件消息后，**只负责发布领域事件**（`SmokeReportedEvent`、`DeviceHeartbeatEvent` 等），由 `DeviceEventListeners` 监听并调用业务 Service 处理。这样 MQTT 网关层不反向依赖业务层，彻底避免了循环依赖。

---

## 3. 目录结构

```
smart-smoke-sensor/
├── sql/
│   ├── schema.sql              # 建库建表脚本（含默认阈值数据）
│   └── reset-data.sql          # 数据重置脚本（清空业务数据）
├── src/main/java/com/cqu/
│   ├── SmartSmokeSensorApplication.java   # 启动类
│   ├── common/                 # 公共组件
│   │   ├── annotation/         #   RequireRole（角色权限注解）
│   │   ├── enums/              #   枚举（7 个：角色/告警等级/告警类型/设备类型等）
│   │   └── exception/          #   业务异常 + 错误码
│   ├── config/                 # 配置层（10 个类）
│   ├── controller/             # 接口层（7 个 Controller）
│   ├── entity/                 # 实体层（7 个实体，对应 7 张表）
│   ├── mapper/                 # 数据访问层（7 个 Mapper）
│   ├── event/                  # 领域事件（5 个类）
│   ├── schedule/               # 定时任务（3 个）
│   ├── service/                # 业务层（9 接口 + 8 实现）
│   ├── utils/                  # 工具类（4 个）
│   └── vo/                     # 视图对象（21 个 VO）
├── src/main/resources/
│   ├── mapper/
│   │   └── KnowledgeChunksMapper.xml   # RAG 向量检索 SQL
│   ├── application.yml                 # 公开配置
│   └── application-secret-example.yml  # 敏感配置模板（需复制为 application-secret.yml）
├── API文档.md                  # 接口文档
├── pom.xml                     # Maven 依赖
└── .gitignore
```

---

## 4. 各层模块详解

### 4.1 common（公共组件）

| 子包 / 类 | 职责 |
|-----------|------|
| `annotation/RequireRole` | 自定义注解，标注接口所需角色（如 `@RequireRole({Role.SYSTEM_ADMIN})`） |
| `enums/` | 7 个枚举：`Role`（角色）、`AlarmLevel`（告警等级）、`AlarmType`（告警类型）、`AlarmStatus`（告警状态）、`DeviceType`（设备类型）、`OnlineStatus`（在线状态）、`Disposition`（处置结论） |
| `exception/BusinessException` | 业务异常，携带错误码与提示信息 |
| `exception/ErrorCode` | 统一错误码定义（400/401/403/404/500） |

### 4.2 config（配置层）

| 类 | 职责 |
|----|------|
| `WebMvcConfiguration` | 注册 HTTP 认证拦截器、角色拦截器、拦截路径与免认证白名单 |
| `HttpAuthInterceptor` | 解析 JWT，写入 `UserHolder` 线程上下文 |
| `RoleInterceptor` | 校验 `@RequireRole` 注解与用户角色 |
| `MqttGateway` | MQTT 客户端：连接、订阅、消息接收 → 发布领域事件；实现 `IDeviceCommander` 下发指令 |
| `WebSocketConfig` | STOMP 端点 `/ws`、消息代理 `/topic` |
| `WebSocketAuthInterceptor` | WebSocket 握手时的 token 校验 |
| `GlobalExceptionHandler` | 全局异常处理，统一返回 Result |
| `MyBatisPlusConfig` | 分页插件、下划线驼峰映射 |
| `PgVectorTypeHandler` | pgvector 向量字段与 Java 数组的类型转换 |
| `LlmConfig` | 大模型 / Embedding 客户端 Bean 配置 |

### 4.3 controller（接口层）

| Controller | 路径前缀 | 功能 |
|-----------|---------|------|
| `UsersController` | `/users` | 注册、登录 |
| `DevicesController` | `/devices` | 设备 CRUD、统计、详情、心跳、自检 |
| `SmokeReadingsController` | `/smoke-readings` | 烟雾记录查询、最新值、趋势、上报 |
| `AlarmLogsController` | `/alarm-logs` | 告警查询、统计、详情、解决、确认、处置 |
| `ThresholdConfigController` | `/threshold-config` | 阈值获取、更新 |
| `ControlLogsController` | `/control-logs` | 控制审计日志查询 |
| `KnowledgeChunksController` | `/knowledge-chunks` | RAG 问答、知识导入 |

> 完整接口参数与返回格式见 [API文档.md](API文档.md)。

### 4.4 service（业务层）

| 接口 | 实现类 | 职责 |
|------|--------|------|
| `IUsersService` | `UsersServiceImpl` | 注册、登录、JWT 签发、密码加密 |
| `IDevicesService` | `DevicesServiceImpl` | 设备 CRUD、心跳/自检处理、在线状态、统计 |
| `ISmokeReadingsService` | `SmokeReadingsServiceImpl` | 烟雾上报、记录查询、趋势、最新值 |
| `IAlarmLogsService` | `AlarmLogsServiceImpl` | 告警 CRUD、解决、确认、处置、统计 |
| `IAlertRuleEngine` | `AlertRuleEngineImpl` | **核心规则引擎**：分级判定 + 多参数 + 消抖 |
| `IThresholdConfigService` | `ThresholdConfigServiceImpl` | 阈值配置读写 |
| `IControlLogsService` | `ControlLogsServiceImpl` | 控制审计日志 |
| `IKnowledgeChunksService` | `RagServiceImpl` | 知识导入、向量化、RAG 检索 + LLM 问答 |
| `IDeviceCommander` | `MqttGateway` | 设备指令下发抽象接口 |

### 4.5 entity + mapper（数据层）

7 张表对应 7 个实体与 Mapper：

| 实体 | 表名 | 说明 |
|------|------|------|
| `Users` | `users` | 用户（含角色、归属小区） |
| `Devices` | `devices` | 设备（序列号、类型、位置、电量、在线状态） |
| `SmokeReadings` | `smoke_readings` | 烟雾采样记录（烟雾/温度/CO 浓度） |
| `AlarmLogs` | `alarm_logs` | 告警记录（类型、等级、状态、处置结论） |
| `ThresholdConfig` | `threshold_config` | 阈值配置（单行，含消抖/升级参数） |
| `ControlLogs` | `control_logs` | 操作审计日志 |
| `KnowledgeChunks` | `knowledge_chunks` | 知识库（含 pgvector 向量） |

### 4.6 event（领域事件）

| 类 | 职责 |
|----|------|
| `SmokeReportedEvent` | 烟雾上报事件 |
| `DeviceHeartbeatEvent` | 设备心跳事件 |
| `DeviceSelfCheckEvent` | 设备自检事件 |
| `DeviceAlarmReportedEvent` | 硬件主动告警事件 |
| `DeviceEventListeners` | 事件监听器，调用对应业务 Service 处理 |

### 4.7 schedule（定时任务）

| 类 | 频率 | 职责 |
|----|------|------|
| `HeartbeatCheckTask` | 每 30 秒 | 扫描在线设备，心跳超时则标记离线并产生离线告警 |
| `BatteryCheckTask` | 每 5 分钟 | 扫描低电量设备，产生低电量告警 |
| `AlertEscalationTask` | 每 1 分钟 | 火警超时未确认则自动升级 |

### 4.8 vo（视图对象）

请求体（`Request` 后缀）与响应体（`VO` 后缀）分离，共 21 个类，涵盖登录、设备、烟雾、告警、阈值、控制日志、RAG、分页与 WebSocket 消息。

### 4.9 utils（工具类）

| 类 | 职责 |
|----|------|
| `JwtProperties` | JWT 密钥配置读取 |
| `UserHolder` | 线程上下文，存储当前登录用户 |
| `CurrentUser` | 从 `UserHolder` 获取用户信息（含数据权限判断） |
| `WebSocketNotifier` | WebSocket 推送统一封装（烟雾/设备状态/告警） |

---

## 5. 核心业务流程

### 5.1 烟雾上报 → 分级告警（主链路）

```
硬件(MQTT/HTTP) ──上报──▶ MqttGateway / SmokeReadingsController
                              │
                              ▼
                      SmokeReportedEvent（领域事件）
                              │
                              ▼
                  SmokeReadingsServiceImpl.reportReading()
                    1. 保存烟雾记录
                    2. 刷新设备在线状态
                    3. WebSocket 推送
                              │
                              ▼
                   AlertRuleEngineImpl 规则引擎
                    1. 读取阈值配置
                    2. 分级判定：超报警阈值 or 超预警阈值
                    3. 多参数交叉验证（可选：温度/CO）
                    4. 消抖：连续 N 次超阈值才触发
                    5. 告警去重（同类型活跃告警不重复创建）
                              │
                              ▼
                      创建 AlarmLogs + WebSocket 推送告警
```

### 5.2 告警升级

`AlertEscalationTask` 每分钟扫描：`FIRE` 级别且 `ACTIVE` 且未被确认、超过阈值分钟数的告警 → 标记 `escalated=true`，推送 `ALARM_ESCALATED`。

### 5.3 心跳离线检测

`HeartbeatCheckTask` 扫描在线设备，若 `lastHeartbeatTime` 距今超过 `heartbeatTimeout`（默认 60 秒）→ 设备置为离线，产生 `OFFLINE` 告警。

### 5.4 RAG 问答

`/knowledge-chunks/chat`：知识导入时向量化存入 `knowledge_chunks` → 提问时用 pgvector 检索 Top-K 相关片段 → 拼接上下文调用 LLM 生成回答。

---

## 6. 数据库设计（7 张表）

| 表 | 关键字段 | 说明 |
|----|---------|------|
| `users` | username, password, role, community_id | 用户与角色、小区归属 |
| `devices` | device_sn, device_type, community_id, location, battery_level, online_status, last_heartbeat_time | 设备与状态 |
| `smoke_readings` | device_id, smoke_concentration, temperature, co_concentration | 烟雾采样 |
| `alarm_logs` | device_id, alarm_type, alarm_level, message, status, disposition, acknowledged_at, escalated | 告警全生命周期 |
| `threshold_config` | smoke_warn/alarm_threshold, temperature_threshold, co_threshold, heartbeat_timeout, battery_low_threshold, debounce_count, escalation_minutes, multi_param_enabled | 单行阈值配置 |
| `control_logs` | device_id, operator_id, command, source, result | 操作审计 |
| `knowledge_chunks` | title, content, embedding(vector) | 知识库 + 向量 |

> 建表脚本见 [sql/schema.sql](sql/schema.sql)。

---

## 7. 关键设计决策

1. **事件驱动解耦**：MQTT 网关与业务层通过领域事件通信，消除循环依赖，便于单测与替换通信通道。
2. **RBAC + 数据权限**：`@RequireRole` 注解做角色控制，Service 层按 `communityId` 做小区数据隔离。
3. **消抖 + 多参数判定**：烟感对误报敏感，连续 N 次超阈值 + 多参数交叉验证是降误报的核心手段。
4. **统一响应与异常**：`Result` / `PageResult` 统一返回结构，`BusinessException` + `GlobalExceptionHandler` 统一错误处理，兜底异常不泄露内部信息。
5. **ID 字符串化**：所有标识字段返回字符串，避免前端大数精度丢失。
6. **敏感配置分离**：数据库密码、JWT 密钥、LLM Key 放在 `application-secret.yml`（已 gitignore），模板见 `application-secret-example.yml`。
