# 智慧烟感 — 负责人机制与多小区权限设计

> 本文档是"负责人机制"落地的设计说明，不直接改代码。评审通过后再按「九、实施步骤」拆解实现。

## 一、背景与问题

当前系统的用户/权限层存在以下问题，导致各表"孤立"，只专注于传感器数据处理：

1. **小区没有实体表**：`community_id` 只是 `users`/`devices` 上的裸 `BIGINT`，系统内无法维护小区元信息（名称、地址、负责人），也无法做小区级 CRUD。
2. **没有用户统一管理能力**：`UsersController` 只有注册/登录，系统管理员无法增删改查用户、分配角色、分配小区、重置密码、启停用。
3. **注册存在越权漏洞**：`UsersServiceImpl.register` 直接信任前端传入的 `role` 和 `community_id`，任何人都能把注册成 `SYSTEM_ADMIN`。
4. **告警推送是全局广播**：`WebSocketNotifier` 对 `/topic/alarms` 全局广播，未按角色/小区/告警类型区分，导致"设备离线"这类消息也推送给住户和消防员。
5. **数据权限过滤不统一**：仅 `DevicesServiceImpl` 对居民/小区管理员做了 `community_id` 过滤，告警等其他资源未统一收敛。

## 二、角色体系（负责人机制）

沿用现有 4 角色（`Role` 枚举），并明确其"负责人"职责：

| 角色 | 枚举值 | 定位 | 数据范围 |
|---|---|---|---|
| 居民 | `RESIDENT` | 住户，查看本小区设备状态与告警，家中报警器重点提示 | 本小区只读 |
| 小区管理员 | `COMMUNITY_ADMIN` | 小区负责人，管理本小区设备与用户、处置告警 | 本小区读写 |
| 消防员 | `FIREFIGHTER` | 接收全部小区火警，跨小区处置 | 跨小区只读（仅火警） |
| 系统管理员 | `SYSTEM_ADMIN` | 总管理员，管理全部小区、全部用户、全部设备 | 全量读写 |

负责人关系：**系统管理员 → 各小区（小区管理员）→ 本小区用户/设备**；消防员为横向应急角色，不参与用户管理。

## 三、权限矩阵

### 3.1 功能权限

| 功能 | RESIDENT | COMMUNITY_ADMIN | FIREFIGHTER | SYSTEM_ADMIN |
|---|:---:|:---:|:---:|:---:|
| 查看本小区设备/告警数据 | ✓ | ✓ | ✓ | ✓ |
| 查看全部小区设备/数据 | ✗ | ✗ | ✗ | ✓ |
| 设备增删改（本小区） | ✗ | ✓ | ✗ | ✓ |
| 设备增删改（全部） | ✗ | ✗ | ✗ | ✓ |
| 绑定/解绑住户-设备（本小区） | ✗ | ✓ | ✗ | ✓ |
| 告警处置（确认/误报/解决） | ✗ | ✓ | ✓（仅火警） | ✓ |
| 下发联动指令（本小区） | ✗ | ✓ | ✗ | ✓ |
| 阈值配置 | ✗ | ✗ | ✗ | ✓ |
| 用户管理（本小区） | ✗ | ✓ | ✗ | ✓ |
| 用户管理（全部） | ✗ | ✗ | ✗ | ✓ |
| 小区管理（增删改） | ✗ | ✗ | ✗ | ✓ |
| RAG 知识库管理 | ✗ | ✗ | ✗ | ✓ |

### 3.2 告警可见与提示矩阵（核心）

> 住户可见本小区**全部**告警；其中**绑定设备（家中报警器）**的告警重点提示。

| 告警类型 | 告警等级 | RESIDENT | COMMUNITY_ADMIN | FIREFIGHTER | SYSTEM_ADMIN |
|---|:---:|:---:|:---:|:---:|:---:|
| `SMOKE_HIGH` / `TEMP_HIGH` / `CO_HIGH`（火警） | FIRE | ✓（本小区，绑定重点提示） | ✓（本小区） | ✓（全量） | ✓ |
| `OFFLINE`（设备离线） | OFFLINE | ✓（本小区可查） | ✓（本小区） | ✗ | ✓ |
| `LOW_BATTERY`（低电量） | LOW_BATTERY | ✓（本小区，绑定重点提示） | ✓（本小区） | ✗ | ✓ |
| `SENSOR_FAULT`（传感器故障） | FAULT | ✓（本小区可查） | ✓（本小区） | ✗ | ✓ |
| 预警（WARN） | WARN | ✓（本小区可查） | ✓（本小区） | ✗ | ✓ |

规则要点：
- **可见**：住户可查询本小区全部告警（含离线/故障/低电量），完整掌握设备状态。
- **重点提示**：住户绑定设备（家中报警器）的火警/低电量，通过定向推送重点提醒；非绑定设备的告警仅在列表中可查、不实时打扰。
- **火警**跨角色、跨小区广播（消防员需接收全部小区火警）。
- **消防员**仅关注火警，不接收离线/低电量/故障等运维类消息。
- 实现时通过**告警类型 → 目标角色/小区**的映射表驱动，避免硬编码。

## 四、数据库设计

### 4.1 新增 `community` 小区表

```sql
CREATE TABLE IF NOT EXISTS community
(
    id           BIGSERIAL PRIMARY KEY,
    name         VARCHAR(128) NOT NULL,           -- 小区名称
    address      VARCHAR(256),                    -- 小区地址
    admin_user_id BIGINT,                         -- 负责人（小区管理员）用户 ID，可空
    created_at   TIMESTAMP    NOT NULL DEFAULT now()
);
COMMENT ON TABLE community IS '小区表';
COMMENT ON COLUMN community.admin_user_id IS '小区负责人（COMMUNITY_ADMIN 用户 ID）';
```

关联关系：
- `users.community_id` → `community.id`
- `devices.community_id` → `community.id`
- `community.admin_user_id` → `users.id`（负责人，可选，一个小区可多个管理员时此字段可留空并另建关联表）

### 4.2 `users` 表增强（认证/审核）

在现有 `users` 表上新增字段：

```sql
ALTER TABLE users
    ADD COLUMN IF NOT EXISTS status VARCHAR(16) NOT NULL DEFAULT 'ACTIVE',  -- PENDING | ACTIVE | DISABLED
    ADD COLUMN IF NOT EXISTS real_name VARCHAR(64),                          -- 真实姓名（审核用）
    ADD COLUMN IF NOT EXISTS phone VARCHAR(32);                              -- 联系电话（审核用）
```

- `PENDING`：注册待审核，不可登录。
- `ACTIVE`：正常。
- `DISABLED`：禁用，不可登录。

### 4.3 告警数据权限关联

`alarm_logs` 不新增 `community_id` 字段，改为通过 `device_id` JOIN `devices` 获取小区，避免数据冗余；数据权限过滤时统一走该 JOIN。

### 4.4 新增 `user_device` 住户-设备绑定表（用于重点提示）

> 住户可见本小区全部告警；本表用于标记"住户家中的报警器"，使该设备告警对住户**重点提示**，而非权限隔离。

```sql
CREATE TABLE IF NOT EXISTS user_device
(
    id         BIGSERIAL PRIMARY KEY,
    user_id    BIGINT NOT NULL,   -- 住户（RESIDENT）
    device_id  BIGINT NOT NULL,   -- 烟感设备
    created_at TIMESTAMP NOT NULL DEFAULT now(),
    UNIQUE (user_id, device_id)   -- 同一住户对同一设备只绑定一次
);
COMMENT ON TABLE user_device IS '住户与其家中烟感设备的绑定关系表（用于告警重点提示）';
CREATE INDEX IF NOT EXISTS idx_user_device_user ON user_device (user_id);
CREATE INDEX IF NOT EXISTS idx_user_device_device ON user_device (device_id);
```

绑定规则：
- 仅可绑定 `RESIDENT`（住户）与设备；其他角色不走此表。
- 设备必须属于该住户所在小区（`devices.community_id = users.community_id`），防止跨小区误绑。
- 一个住户可绑定多台设备；同一设备可绑定给同户多个成员。
- 绑定仅影响"重点提示"，不影响"可见范围"（住户仍可见本小区全部）。

## 五、注册与认证机制

### 5.1 注册分级策略

| 目标角色 | 能否自助注册 | 由谁创建 | 初始状态 |
|---|---|---|---|
| `RESIDENT` | ✓ | 本人（需选小区） | `PENDING`（待本小区管理员/系统管理员审核） |
| `COMMUNITY_ADMIN` | ✗ | `SYSTEM_ADMIN` 创建 | `ACTIVE` |
| `FIREFIGHTER` | ✗ | `SYSTEM_ADMIN` 创建 | `ACTIVE` |
| `SYSTEM_ADMIN` | ✗ | 系统初始化 seed（默认账号） | `ACTIVE` |

### 5.2 注册与审核流程

1. 居民提交 `register`：只能注册为 `RESIDENT`，必填 `community_id`、`real_name`、`phone`，`status = PENDING`。
2. 后端**强制覆盖** `role = RESIDENT`（忽略前端传入），杜绝越权注册。
3. 小区管理员/系统管理员在"用户管理"中对 `PENDING` 用户执行审核（通过→`ACTIVE`，拒绝→删除或 `DISABLED`）。
4. 登录时校验 `status == ACTIVE`，否则拒绝并提示"账号待审核/已禁用"。
5. 审核通过后，管理员可（可选）在"设备绑定管理"中为该住户绑定其家中报警器，用于告警重点提示。

### 5.3 登录校验增强

`login` 增加状态校验：`PENDING` → "账号待审核"，`DISABLED` → "账号已被禁用"。

## 六、管理接口

新增 `CommunityController`，扩展 `UsersController`，并新增设备绑定能力（均需鉴权）。

### 6.1 小区管理（`SYSTEM_ADMIN`）

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/community` | 新增小区 |
| GET | `/community` | 小区列表（分页） |
| PUT | `/community/{id}` | 修改小区 |
| DELETE | `/community/{id}` | 删除小区（无设备/用户时） |
| PUT | `/community/{id}/admin` | 指定/更换小区负责人 |

### 6.2 用户管理

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| GET | `/users` | ADMIN | 用户分页列表（按角色/小区/状态筛选） |
| POST | `/users` | ADMIN | 管理员创建用户（可指定角色/小区） |
| PUT | `/users/{id}` | ADMIN | 编辑用户（角色、小区、真实姓名、电话） |
| PUT | `/users/{id}/status` | ADMIN | 启用/禁用 |
| PUT | `/users/{id}/password` | ADMIN | 重置密码 |
| PUT | `/users/{id}/audit` | COMMUNITY_ADMIN / SYSTEM_ADMIN | 审核注册（通过/拒绝） |
| DELETE | `/users/{id}` | SYSTEM_ADMIN | 删除用户 |

数据权限：`COMMUNITY_ADMIN` 只能操作本小区（`community_id` 相等）用户；`SYSTEM_ADMIN` 操作全部。

### 6.3 住户-设备绑定管理

| 方法 | 路径 | 权限 | 说明 |
|---|---|---|---|
| PUT | `/devices/{deviceId}/bind` | COMMUNITY_ADMIN / SYSTEM_ADMIN | 绑定住户到设备（body: userId） |
| PUT | `/devices/{deviceId}/unbind` | COMMUNITY_ADMIN / SYSTEM_ADMIN | 解绑住户与设备（body: userId） |
| GET | `/users/{userId}/devices` | COMMUNITY_ADMIN / SYSTEM_ADMIN / 本人 | 查询住户绑定的设备列表 |

## 七、告警消息路由设计

### 7.1 现状

`WebSocketNotifier` 对固定主题全局广播，无角色/小区维度。

### 7.2 目标

将推送从"全局广播"改为"按角色 + 小区 + 告警类型路由"，并支持住户绑定设备的**重点提示**。

### 7.3 主题设计

| 主题 | 订阅者 | 用途 |
|---|---|---|
| `/topic/alarms/fire` | 全部角色 | 火警（跨小区广播） |
| `/topic/community/{communityId}/alarms` | 本小区 RESIDENT/COMMUNITY_ADMIN | 本小区告警（可见层，含离线/低电量/故障） |
| `/topic/community/{communityId}/devices` | 本小区 COMMUNITY_ADMIN | 设备状态与自检（运维） |
| `/topic/community/{communityId}/smoke` | 本小区 RESIDENT/COMMUNITY_ADMIN | 烟雾读数 |
| `/user/{userId}/queue/alerts` | 单个住户 | 绑定设备告警的**重点提示**（定向推送） |

### 7.4 路由规则实现

- 告警产生时，按 `AlarmType` 查「3.2 矩阵」，得到目标角色集合。
- 结合 `alarm_logs → devices.community_id` 得到小区，向对应主题推送：
  - 火警 → `/topic/alarms/fire`（跨小区）+ 本小区主题。
  - 离线/低电量/故障/预警 → `/topic/community/{id}/alarms`。
- **重点提示**：若告警设备属于某住户的绑定设备（查 `user_device`），额外通过 `convertAndSendToUser` 向该住户的 `/queue/alerts` 定向推送（火警/低电量）。
- 消防员仅订阅 `/topic/alarms/fire`，不会收到离线/低电量等运维消息。

## 八、数据权限统一过滤方案

封装统一的 `DataScope`（数据权限）组件，替代各 Service 手写过滤：

```
DataScope.resolve(currentUser) → DataScopeContext {
    all: boolean,                  // 是否全量（SYSTEM_ADMIN / FIREFIGHTER 读全量）
    communityId: Long|null,        // 受限小区（RESIDENT / COMMUNITY_ADMIN）
    boundDeviceIds: List<Long>|null // 住户绑定设备（RESIDENT，仅用于重点提示，非过滤）
}
```

- `SYSTEM_ADMIN`：`all = true`，查全量。
- `FIREFIGHTER`：火警类资源 `all = true`，其余资源不授权访问。
- `COMMUNITY_ADMIN` / `RESIDENT`：`communityId = UserHolder.getCommunityId()`，查询时 `WHERE community_id = ?`（住户与本小区管理员可见范围相同，均为本小区）。
- `RESIDENT` 的 `boundDeviceIds`：从 `user_device` 查得，用于告警"重点提示"标记与定向推送，**不用于过滤可见范围**。

落地点：
1. `DevicesServiceImpl` 现有过滤逻辑迁移到 `DataScope`。
2. `AlarmLogsServiceImpl` 查询统一 `JOIN devices` 后按 `DataScope` 过滤（住户可见本小区全部）。
3. 告警推送时结合 `user_device` 计算 `boundDeviceIds`，触发住户重点提示。
4. 小区/用户管理接口按 `DataScope` 限定操作范围。

## 九、实施步骤（分阶段任务清单）

> 按以下 9 个阶段逐个实现，每阶段编译验证通过后再进入下一阶段。依赖：阶段 0 → 1 →（2/3/4/5/6 可并行）→ 7 → 8 → 9。

### 阶段 0 · 数据库层
- 修改 `sql/schema.sql`：新增 `community` 表、`user_device` 表，`users` 加 `status`/`real_name`/`phone`
- 修改 `sql/reset-data.sql`：清空新表 + 恢复阈值

### 阶段 1 · 实体与 Mapper
- 新建 `Community.java`、`CommunityMapper.java`
- 新建 `UserDevice.java`、`UserDeviceMapper.java`
- 修改 `Users.java`：加 `status`/`realName`/`phone`
- 新建 `DataInitializer`：启动时 seed 默认 `SYSTEM_ADMIN`（admin/123456）与示例小区

### 阶段 2 · 注册安全 + 登录状态校验
- 修改 `RegisterRequest.java`：加 `realName`/`phone`
- 修改 `UsersServiceImpl.register`：强制 `RESIDENT` + `PENDING`
- 修改 `UsersServiceImpl.login`：状态校验

### 阶段 3 · 小区管理
- 新建 `ICommunityService` + `CommunityServiceImpl`、`CommunityVO`
- 新建 `CommunityController`（CRUD + 指定负责人）

### 阶段 4 · 用户统一管理
- 修改 `IUsersService` + `UsersServiceImpl`：分页/创建/编辑/启停/重置密码/审核
- 新建 `UserVO`、`UserCreateRequest`、`UserUpdateRequest`
- 修改 `UsersController`

### 阶段 5 · 住户-设备绑定
- 新建 `IUserDeviceService` + `UserDeviceServiceImpl`、`BindDeviceRequest`
- 修改 `DevicesController`：bind / unbind / 列表

### 阶段 6 · DataScope 数据权限
- 新建 `DataScope`
- 修改 `DevicesServiceImpl` / `AlarmLogsServiceImpl` / `SmokeReadingsServiceImpl` 接入

### 阶段 7 · 告警消息路由
- 新建告警类型→角色映射
- 修改 `WebSocketNotifier`、`AlarmLogsServiceImpl.createAlarm`

### 阶段 8 · 鉴权注解补齐
- 修改各 Controller 补 `@RequireRole`
- 修改 `WebSocketAuthInterceptor` 写入 role/communityId

### 阶段 9 · 编译验证 + 全链路自测
- `mvn compile`；重启后走通注册审核→建小区→绑设备→住户看告警→重点提示

## 十、边界与待确认

- **阈值配置**：当前 `threshold_config` 为全局单行；若小区需独立阈值，需后续扩展为 `community_id` 维度（本期不涉及）。
- **一个小区多管理员**：当前 `community.admin_user_id` 为单值；如需多负责人，建议后续改为 `community_admin` 关联表。
- **可视化（变化曲线）**：本期不做，维持现状（历史列表 + `/smoke-readings/trend` 单指标烟雾浓度曲线雏形）。曲线渲染由前端负责，后端仅提供趋势数据接口；如需多指标（烟雾/温度/CO）或多设备对比曲线，后续再扩展趋势接口返回多条序列。
- **审核通知**：审核结果可通过 WebSocket/站内信通知用户，本期可先不做，仅登录时提示状态。
